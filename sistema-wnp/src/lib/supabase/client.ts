// Stub — Supabase ainda não está conectado (Fase K do roadmap, ver MASTER_CONTEXT.md).
// Quando entrar, os `*.repository.ts` em src/lib/data trocam a leitura de
// src/lib/mock por chamadas a este client — nenhum componente precisa mudar.
//
// import { createBrowserClient } from "@supabase/ssr";
//
// export function createClient() {
//   return createBrowserClient(
//     process.env.NEXT_PUBLIC_SUPABASE_URL!,
//     process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
//   );
// }
